# SQLite database file (relative to cfo-dashboard/)
DATABASE_URL="file:./dev.db"

# Optional — Stripe integration. App works fine with these unset (empty state).
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=
